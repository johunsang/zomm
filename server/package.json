{
  "name": "@zomm/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "zomm 상태 비저장 프록시 — 브라우저가 보낸 Cloudflare 키로 RealtimeKit REST API를 중계한다",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.3",
    "dotenv": "^16.4.5",
    "hono": "^4.12.22"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
